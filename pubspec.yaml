name: mkbsd
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  http: ^1.1.0
  path: ^1.9.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
